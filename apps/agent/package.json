{
  "name": "agent",
  "version": "0.0.1",
  "type": "module",
  "main": "src/graph.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "agent": "langgraphjs dev"
  },
  "devDependencies": {
    "@repo/typescript-config": "*",
    "typescript": "^6.0.0"
  }
}